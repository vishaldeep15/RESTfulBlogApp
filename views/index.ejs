<h1>Index Page</h1>

<% blogs.forEach(function(blog){ %>
	<div>
		<h2> <%= blog.name %>
		<img src="<%= blog.image %>">
		<span> <%= blog.created %> </span>
		<p>	<%= blog.body %> </p>
	</div>

<% }); %>

